{{ 'customer.css' | asset_url | stylesheet_tag }}

{% comment %} <script src="{{ 'customer.js' | asset_url }}" defer></script> {% endcomment %}

<div class="customer portal section-{{ section.id }}-padding">
  <div class="grid grid-cols-12 h-full gap-0">
    <div class="col-span-12 lg:col-span-3 xl:col-span-2">
      {% render 'account-sidebar', active: 'portal' %}
    </div>
    <div class="col-span-12 lg:col-span-9 xl:col-span-10">
      <div class="content--wrapper">
        <div data-address class="content--inner">
          <div class="content-header">
            <h2 class="content--title">Klantenzone</h2>
          </div>
          <div class="content-main portal-main space-y-14">
            <div class="portal-intro">
              <h2 class="intro">
                <span>Welkom in jouw klantenzone!</span> <br>
                <br>
                We werken momenteel hard om deze zone te voorzien van handige links en bestanden, zodat jij
                gemakkelijker toegang hebt tot alle benodigde informatie.
              </h2>
              <h2 class="intro">
                <span>Vragen, suggesties of feedback? </span> <br>
                <br>
                Heb je vragen, suggesties of feedback? We horen graag van je! Jouw input helpt ons om de klantenzone nog
                beter te maken. Aarzel niet om contact met ons op te nemen via
                <a href="mailto:tania@gloskinbeauty.be">tania@gloskinbeauty.be</a>. We staan klaar om je te ondersteunen
                en te luisteren naar wat jij te zeggen hebt.
              </h2>
            </div>
            <div class="portal-item">
              <h2>Belangrijke mappen</h2>
              {% if linklists.dropbox != empty %}
                <div class="dir-links">
                  {% for item in linklists.dropbox.links %}
                    <a target="_blank" class="dir--link" href="{{ item.url }}">
                      {% render 'icon-directory' %}
                      <p>{{ item.title }}</p>
                    </a>
                  {% endfor %}
                </div>
              {% endif %}
            </div>

            {% comment %}
              <div class="portal-item">
                <h2>Handige Bestanden</h2>
                {% if linklists.dropbox != empty %}
                  <div class="dir-links">
                    {% for item in linklists.dropbox.links %}
                      <a target="_blank" class="dir--link" href="{{ item.url }}">
                        {% render 'icon-file' %}
                        <p>{{ item.title }}</p>
                      </a>
                    {% endfor %}
                  </div>
                {% endif %}
              </div>
            {% endcomment %}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
